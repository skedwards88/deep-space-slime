import {getValidNextIndexes} from "./getValidNextIndexes";
import {features, numColumns, numRows} from "./constants";

const allBasicPuzzle = Array.from(
  {length: numColumns * numRows},
  () => features.basic,
);

describe("getValidNextIndexes", () => {
  test("todo", () => {});
});
